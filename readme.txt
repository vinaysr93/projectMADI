Run the app.py for the main program. All html files are present in templates.
Quantified.sqlite3 is the database name.
Requirements.txt has the dependencies required for running the program. 
models.py has the database models. 